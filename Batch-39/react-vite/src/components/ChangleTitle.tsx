import { useLocation } from "react-router-dom";

const ChangleTitle = () => {
  const location = useLocation();
  console.log("<<=== 🚀 location ===>>", location);
  return <></>;
};

export default ChangleTitle;
